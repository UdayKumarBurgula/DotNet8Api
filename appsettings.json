{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Export": {
    "FilePath": "upload/export.json" // relative to the app content root OR use an absolute path
  }
}